# Power BI Folder

This folder is intended to hold the Power BI dashboard file:

- `churn_dashboard.pbix` - Power BI churn analysis dashboard (add your file here)

> Note: .pbix files are binary and should be uploaded directly via the GitHub web interface (Add file > Upload files) or via git push.
